export const mutations = {
    change (state, value) {
        // mutate state
        state.result = value
      },
    changePromo (state, value) {
    // mutate state
    state.promo = value
    }
}